apiVersion: v1
kind: Service
metadata:
  name: socialcert-backend
  labels:
    app.kubernetes.io/name: socialcert-backend
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: socialcert-backend
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
